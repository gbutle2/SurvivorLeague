/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: Keep this file free of imports from `.next/types/*` so
 // `npm run typecheck` works on a clean checkout before `next build`.
// `next dev` / `next build` may rewrite this file; restore the
// reference-only form before committing if that happens.
